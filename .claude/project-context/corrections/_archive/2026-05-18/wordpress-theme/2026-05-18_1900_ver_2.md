module: wordpress-theme
affects: [blog, courses, contact-form]
type: refactor
version: 1.2
date: 2026-05-18
manual: false

## Changes
- **Theme relocated from the `next-level-academy/` subfolder to the repository root.** The repo root is now the theme root — drop the whole repo into `wp-content/themes/`. Update module line "Theme folder: `next-level-academy/`" to "Theme root = repository root".
- Original static HTML source files (`index.html`, `course.html`, `course-details.html`, `blog.html`, `blog-details.html`, `about-us.html`, `contact-us.html`) **deleted** — conversion is complete and sources are no longer kept. The "Source HTML" column in the Template files table is now historical reference only.
- Old nested `next-level-academy/` tree (templates + duplicated assets) removed from the working tree.
- Concrete brand palette now defined in root `style.css` `:root`: `--color-primary:#a53692`, `--color-secondary:#faa831`, `--color-secondary-alt:#FFF6F4`, `--color-tertiary:#FFA41B`, `--color-heading`/`--color-dark`:`#543874`, `--color-body:#6F6B80`.
- Two stylesheets enqueued in `functions.php`: `assets/css/style.css` as handle `nla-main` (design system) and root `/style.css` as handle `nla-custom` (WP theme header + CF7 overrides). Document both.
- New `assets/img/` directory added (alongside existing `assets/images/`); currently holds `Next-Level-Academy-process-steps.jpg`.
- Theme/site logos (`assets/images/logo/logo.png`, `logo-white.png`) updated to Next Level Academy branding.

## Impact
- Any doc/instruction referencing the `next-level-academy/` path prefix is stale — paths are now root-relative.
- `NLA_URI` (= `get_template_directory_uri()`) now resolves to the repo when the repo is the active theme; asset path convention unchanged (`NLA_URI . '/assets/...'`).
- blog / courses / contact-form modules: template content unchanged, but their files now live at repo root, not under `next-level-academy/`. courses fallback thumbnail `assets/images/course/course-01/course-01.jpg` still resolves (`assets/images/` retained).
- The static HTML site is fully retired; there is no non-WordPress fallback.

## Validation
- `functions.php:5` `define( 'NLA_URI', get_template_directory_uri() )`; enqueue block lines 33–72 confirm `nla-main` = `assets/css/style.css` (line 47) and `nla-custom` = `NLA_URI . '/style.css'` (line 48).
- Root `style.css:1-9` carries the WordPress theme header (Theme Name: Next Level Academy, Version 1.0.0); `:root` palette at lines 14–21.
- `git status`: all template `.php` + root `style.css` present at repo root as untracked; every `next-level-academy/*` path and all `*.html` sources marked deleted.
- `assets/img/` verified via directory listing (single file: `Next-Level-Academy-process-steps.jpg`).

## Rollback
- `git checkout -- .` restores the previous tracked layout (`next-level-academy/` theme folder + static HTML at root); delete the untracked root `.php`/`style.css`/`assets/img/` files.
- No database or schema changes involved (CPT/taxonomy registration unchanged).
