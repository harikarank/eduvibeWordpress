module: blog
affects: [wordpress-theme]
type: bugfix
version: 1.1
date: 2026-05-16
manual: false

## Changes
- Rewrote `archive.php` — corrected multiple structural errors from initial version:
  - Fixed container class: `edu-elements-area` (was wrongly `edu-blog-area`)
  - Fixed card class: `edu-blog blog-type-2 radius-small` (removed incorrect `variation-2`)
  - Fixed category wrapper: `status-group` (was wrongly `status-group status-style-5`)
  - Added missing `.blog-card-bottom` wrapper containing:
    - `.blog-meta` with `icon-calendar-2-line` + `get_the_date('d M, Y')`
    - `.read-more-btn` with `btn-transparent` "Read More" link to `the_permalink()`
  - Added SAL animation delay increment (+50 per card, starting at 150)
- Rewrote `single.php` — corrected major structural errors from initial version:
  - Removed sidebar entirely (original HTML is `col-lg-10 offset-lg-1`, full-width, no sidebar)
  - Removed author box (not in original HTML)
  - Removed comments block (not in original HTML)
  - Fixed wrapper: `blog-details-1 style-variation3`
  - Restored `.content-blog-top` section: `h4.title` → `.content-status-top` flex row (category badge + date) → `.thumbnail.block-alignwide` (featured image)
  - Category badge class: `eduvibe-status status-05 color-primary w-600`, text uppercased
  - Restored `.blog-tag-and-share.mt--50` → `.tag-list.bg-shade` with individual `<a>` tag links
  - Fixed prev/next: uses `.blog-pagination-list.style-variation-2` structure with full post title in `<span>`, not button links
    - Previous: `<i class="ri-arrow-left-s-line">` + `<span>title</span>`
    - Next (class `next-post`): `<span>title</span>` + `<i class="ri-arrow-right-s-line">`
  - Prev/next section guarded by `if ($prev || $next)` — renders only when applicable
  - Tags section guarded by `if ($tags)` — renders only when post has tags

## Impact
- `archive.php`: visual card layout now matches `blog.html` exactly
- `single.php`: layout now matches `blog-details.html` exactly — full-width, no sidebar, correct nav structure
- No database changes; PHP/template only

## Validation
- Verified against `blog.html` lines 162–399: card HTML structure, class names, `blog-card-bottom` wrapper
- Verified against `blog-details.html` lines 152–232: `blog-details-1 style-variation3`, `content-status-top`, `blog-pagination-list style-variation-2 next-post`, `tag-list bg-shade`
- `col-lg-10 offset-lg-1` confirmed in `blog-details.html` line 155

## Rollback
- Restore previous `archive.php` and `single.php` from git: `git checkout HEAD~1 -- next-level-academy/archive.php next-level-academy/single.php`
