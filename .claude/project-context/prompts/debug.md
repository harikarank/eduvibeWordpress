# Debug prompt

Use this shape when reporting a bug so Claude can route correctly via `indexdb.md`.

Symptom:
Reproduction steps:
Expected:
Actual:
Suspected modules:        # so the indexdb can match
Logs / errors:
Last working version (if known):
