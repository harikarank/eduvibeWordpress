Run the following bash commands in sequence. Stop immediately if any command fails and report the error clearly.

```bash
git add .
```

```bash
git commit -m "$ARGUMENTS"
```

```bash
git push origin dev
```

If any command fails, show the exact error output and do not proceed to the next step.
